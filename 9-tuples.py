coordinates = (4, 5, 6)
# Tuples are used to store multiple items in a single variable. Tuple is one of 4 built-in data types in Python used to store collections of data. A tuple is a collection which is ordered and unchangeable(immuteable).

print(coordinates[1])
print(coordinates[2])